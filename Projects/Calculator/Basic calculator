#include<stdio.h>

float add(float a, float b)
{
    return a+b;
}
float sub(float a, float b)
{
    return a-b;
}
float mul(float a , float b)
{
    return a*b;
}
float div(float a , float b)
{
    return a/b;
}

int main()
{
printf("This is my first project in C language\n");
printf("Nothing to flex just a simple calculator\n");
printf("This calculator can be used to perform basic arithmetic operations like addition, subtraction, multiplication and division\n");
int c;

c=1;
while(c==1)
{
    printf("1. Addition\n");
    printf("2. Subtraction\n");
    printf("3. Multiplication\n");
    printf("4. Division\n");
    int choice;
    float a,b;
    printf("Enter your choice: ");
    scanf("%d",&choice);
    printf("Enter the first number:   ");
    scanf("%f",&a);
    printf("Enter the second number:   ");
    scanf("%f",&b);
switch(choice)
{

    case 1:
	printf("The addition of %f and %f is:  %f",a,b,add(a,b));
    break;
    case 2:
	printf("The subraction of %f and %f is: %f  ",a,b,sub(a,b));
    break;
    case 3:
	printf("The multiplication of %f and %f is:  %f",a,b,mul(a,b));
    break;  
    case 4:
    if(b==0)
    {
        printf("Division by zero is not allowed.");
        break;
    }
	printf("The division of %f and %f is: %f",a,b,div(a,b));
    break;
    
    default:
	printf("Invalid choice");	
    
}
printf("\nDo you want to continue? (1 for yes / 0 for no): ");
scanf("%d",&c);
}
printf("THANKS FOR USING THE CALCULATOR");
}